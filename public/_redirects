# Canonicalize www -> apex (single domain)
https://www.natyv.ai/* https://natyv.ai/:splat 301!

# Retired assessment artifact paths now resolve to the live assessment offer.
/sample-ai-opportunity-report /advisory 301!
/sample-ai-opportunity-report.html /advisory 301!
/sample-ai-opportunity-report.pdf /advisory 301!

# Note: SPA fallback for client-side routing is handled automatically by Cloudflare Pages
# for unknown paths via the prerendered HTML files (index.html, about.html, advisory.html).
# 404s for truly unknown paths fall through to NotFound (catch-all route in App.tsx).
